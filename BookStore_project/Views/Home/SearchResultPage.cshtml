
@model IEnumerable<BookStore_project.Models.Book.BookIndexViewModel>

@{
    ViewData["Title"] = "SearchByCategoryName";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Search Book</h1>
<nav aria-label="breadcrumb">
     <ol class="breadcrumb">
     <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">Home</a></li>
     <li class="breadcrumb-item"><a asp-controller="Book" asp-action="SearchPage">Search Page</a></li>
     <li class="breadcrumb-item active" aria-current="page">Book Result</li>
     </ol>
</nav>
<br />

<div class="row">
    @foreach (var item in Model){
    <div class="col-lg-3 col-md-4 mb-3">
        <div class="product-box">
            <div class="product-inner-box position-relative">
                <div class="icons position-absolute">
                    <i class="fa-solid fa-heart"></i>
                    <i class="fa-solid fa-eye"></i>
                </div>
                @*<img src="/img/books/2.jpg" alt="Woodan chair" class="img-fluid">*@
                <img src="@Html.DisplayFor(modelItem => item.Image_URL)" alt="Woodan chair" class="img-fluid">

                <div class="cart-btn">
                    <button class="btn btn-white shadow-sm rounded-pill"><i class="fa-solid fa-cart-shopping"></i> Add to Cart </button>
                </div>

            </div>
            <div class="product-info">
                <div class="product-name">
                    <h3>@Html.DisplayFor(modelItem => item.Name)</h3>
                </div>
                <div class="product-price">
                    <span>@Html.DisplayFor(modelItem => item.Price)</span>
                </div>
            </div>
        </div>
    </div>
    }


</div>

@*<div class="pagination">
    @for (int i = 1; i <= @Model.PageCount; i++)
    {
        @if (Model.PageNumber == i)
        {
            <a asp-action="SearchByAuthorName" asp-route-page="@i" class="active">@i</a>
        }
        else
        {
            <a asp-action="SearchByAuthorName" asp-route-page="@i">@i</a>
        }
    }
</div>
<style>
   .pagination {
        display: inline-block;
    }

        .pagination a {
            color: black;
            float: left;
            padding: 8px 16px;
            text-decoration: none;
            transition: background-color .3s;
          border: 1px solid #ddd;
  margin: 0 4px;
}

.pagination a.active {
  background-color: #4CAF50;
  color: white;
  border: 1px solid #4CAF50;
}

.pagination a:hover:not(.active) {background-color: #ddd;}
</style>*@